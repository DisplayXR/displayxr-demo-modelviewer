build/
installer/*.exe
installer/macos/*.pkg
_artifact/
_package/
*.log

# Android
local.properties
.gradle/
android/build/
android/.cxx/
*.iml

# Android signing keystores — never commit (CI uses GitHub secrets)
*.jks
*.keystore

# Python bytecode caches (compiled artifacts; go stale against .py edits)
__pycache__/
*.py[cod]

# Khronos sample-asset submission staging (generated; regenerate with
# scripts/make_khronos_conformance_assets.py)
khronos_submission/

# Fetched by android/fetch-client-aar.sh from the pinned runtime release.
# A binary that is already published elsewhere must not be vendored here: it would
# drift from dxrClientRuntimeTag silently, which is the whole failure this replaces.
android/libs/
